// import { renderUsers, getData } from "./home.js";

// const select = document.querySelector("#selectFilter");

// select.addEventListener("change", (event) => {
//     const selected = event.target.value;

//     async function filterRegion() {
//         const data = await getData();

//         if (selected === "") {
//             renderUsers(data);
//             return;
//         }

//         const filteredRegion = data.filter(c => c.region === selected);
//         renderUsers(filteredRegion);
//     }

//     filterRegion();
// });

